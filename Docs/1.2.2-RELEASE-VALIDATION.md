# Release validation · 1.2.2

The maintainer authorized publication after panel reset testing on September 20, 2026, then expanded the scope to include Map Library, feature suggestions, clearer Rewind rings and the animated tutorial.

- 186 Swift tests passed in safe render mode, including fast button release, component resets, selective tap/hold transfers, mode dependency isolation, cyclic mode references, combination preservation and factory-only ring migration.
- Five Lua bridge suites passed using the Fengari runtime, including grouped preview, range, missing-value and curve-context checks.
- Native renders inspected for the map library and expanded animated tutorial; previous renders inspected for the expanded Rewind workspace, combination editor and release notes.
- Universal Apple Silicon/Intel build; Developer ID signature verified. Not notarized; the existing Apple-account limitation remains.
- Sparkle archive signature verified; newest release notes embedded in the feed and full history packaged in the app.

Automated and render checks do not establish every physical-panel, display-size or end-to-end user workflow. Community submission requires a user-reviewed GitHub issue and maintainer review. The gallery begins with the official factory map. No real user issue or private map was submitted during testing. Live feed/download verification is recorded after publication.
