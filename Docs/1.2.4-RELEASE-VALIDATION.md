# Release validation · 1.2.4

Physical SVG panel references, authorized for publication on 20 September 2026.

- Quick hold guide and printable reference use the mapper's existing 58-control SVG, with spatial assignments, mode/bank/program/combination states, Turn / Tap and Press / Hold views, full-label inspection, zoom, PNG export and PDF printing.
- Previous Still + Next Still chord tests cover both orders, releases, singleton events, timeout/unrelated-key flushing, resets and synthetic hardware reports. Physical chord timing and focus over Lightroom remain hands-on acceptance checks.
- 199 Swift tests passed with zero failures in safe render mode. Native WK image tests included a bottom-of-document sentinel.
- Isolated Chrome artifact checks confirmed 58 controls in each of 38 default-map diagrams, responsive width, inspector/gesture/zoom behavior, PNG download, 38-page complete PDF and five-page enlarged-region PDF. Physical layouts and enlarged key-bank output were visually inspected.
- Installed toolbar and Help menu checked; native current-panel PNG export succeeded. The installed guide reflected the user's custom program. Automated access to its local-file browser tab was blocked by browser-tool policy; artifact rendering tests and native installed export were verified separately.
- Universal arm64/x86_64 build 202609201752, Developer ID signed with hardened runtime, not notarized. No Apple account or notary attempt was made. Existing signed-but-unnotarized release workflow retained with explicit disclosure.
- Staged, installed and extracted-update executable hashes match. Deep/strict signature verification passed. Sparkle Ed25519 signature, archive size, appcast build, URL and bundled release notes verified. Signed DMG integrity and asset SHA-256 checksums verified.
- Existing app and configuration backed up locally. User profile and home settings unchanged; reconnect added a trail keyframe without removing existing branches or events.
- Private backups, validation artifacts and unrelated planning documents are excluded from publication.
